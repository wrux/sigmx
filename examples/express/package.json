{
  "name": "sigmx-example-express",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "predev": "vite build",
    "dev": "node --watch server.js",
    "prestart": "vite build",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "sigmx": "^0.2.0"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  }
}
