---
title: "sigmx"
description: "A modern, server-driven web framework in a runtime the browser barely notices: signals, streaming patches and a state-preserving morph from a 4.5 KB core, with a build that ships only what your markup uses."
template: splash
hero:
  title: The power of a modern framework, in a runtime the browser barely notices.
  tagline: Your server keeps rendering HTML. sigmx fetches it, morphs it into the page without losing your place, and adds signals, two-way binding and streaming. The build ships only the plugins your markup uses. Zero dependencies.
  actions:
    - text: Get started
      link: /start/installation/
      icon: right-arrow
    - text: See the examples
      link: /examples/
      variant: minimal
---

import SizeHero from '../../components/SizeHero.astro'
import Anatomy from '../../components/home/Anatomy.astro'
import Auto from '../../components/home/Auto.astro'
import Extend from '../../components/home/Extend.astro'
import Origins from '../../components/home/Origins.astro'

<SizeHero />
<Anatomy />
<Auto />
<Extend />
<Origins />
