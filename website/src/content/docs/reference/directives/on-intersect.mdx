---
title: "data-on-intersect"
description: "Run an expression when the element scrolls into (or out of) view."
sidebar: {"order": 14, "label": "on-intersect"}
---
import Live from '../../../../components/Live.astro'
import Cost from '../../../../components/Cost.astro'

Run an expression when the element scrolls into (or out of) view.

## Syntax

```html
data-on-intersect="$seen = true"
data-on-intersect__once__threshold.50="@get('/more')"
```

| part | |
|---|---|
| key | forbidden |
| value | required |

## Modifiers

| modifier | effect |
|---|---|
| `__once` | disconnect after the first run |
| `__exit` | run when the element leaves the viewport instead |
| `__threshold.N` | how much must be visible, in percent (default 0: any pixel) |
| `__debounce / __throttle` | timing |

## Example

<Live html={`
<div data-signals="{ seen: 0 }">
  <p>Scroll this box: <b data-text="$seen"></b> intersections</p>
  <div style="height:6rem;overflow:auto;border:1px solid var(--sl-color-hairline)"><div style="height:9rem"></div><p data-on-intersect="$seen++">I am at the bottom.</p></div>
</div>
`} />

## Notes

Backed by `IntersectionObserver`.

## Register

```ts
import { onIntersect } from 'sigmx/plugins'
```

<Cost name="onIntersect" />
